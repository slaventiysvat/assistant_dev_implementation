# 🚀 Personal Assistant

Персональний помічник для управління контактами та нотатками з підтримкою днів народження.

## 📋 Опис

Це консольний додаток на Python для ефективного управління контактами та нотатками. Програма включає:

- ✅ Управління контактами з підтримкою днів народження
- ✅ Система нотаток з тегами  
- ✅ Валідація даних (номери телефонів, email)
- ✅ Розумний пошук за частковими збігами
- ✅ Кольоровий інтерфейс (якщо встановлена colorama)
- ✅ Автозбереження даних у JSON

## 📦 Встановлення

### Вимоги
- Python 3.8+
- Операційна система: Windows, Linux, macOS

### 1. Клонування репозиторію
```bash
git clone https://github.com/slaventiysvat/assistant_dev_implementation.git
cd assistant_dev_implementation
```

### 2. Встановлення залежностей (опціонально)
```bash
# Для кращого відображення кольорів (рекомендується):
pip install colorama
```

### 3. Як Python пакет
```bash
# Встановити як локальний пакет для розробки:
pip install -e .

# Або скопіювати папку до свого проекту і імпортувати:
from personal_assistant.models import Contact, Note
from personal_assistant.managers import ContactManager, NoteManager
```

## 🚀 Запуск програми

### Після встановлення через pip ✅

Після встановлення через `pip install -e .` або `pip install personal-assistant`:

```bash
# 🎯 Головна команда (працює!):
personal-assistant

# Доступні параметри:
personal-assistant --help       # Показати довідку  
personal-assistant --demo       # Демонстраційний режим
personal-assistant --test       # Швидкий тест функціональності

# Альтернативний спосіб як Python модуль:
python -m personal_assistant
python -m personal_assistant --demo
```

### Запуск без встановлення (з вихідного коду)

#### Windows:
```bash
# Найпростіший спосіб - подвійний клік або:
run.bat

# Демонстраційний режим з готовими даними:
run.bat demo

# Простий startup скрипт:
python start.py
```

#### Linux/macOS:
```bash
# Дати права на виконання (одноразово):
chmod +x run.sh

# Запуск:
./run.sh

# Демонстраційний режим:
./run.sh demo

# Простий startup скрипт:
python start.py
```

#### Універсальний спосіб (Python):
```bash
# Основний запуск:
python main.py

# Доступні параметри:
python main.py --help       # Показати довідку
python main.py --demo       # Демонстраційний режим
python main.py --test       # Швидкий тест функціональності
```

### 🏆 Рекомендовані способи запуску:

1. **`personal-assistant`** - після встановлення pip (✅ працює!)
2. **`python -m personal_assistant`** - як Python модуль (✅ працює!)
3. **`python main.py`** - з вихідного коду
4. **`run.bat`** / **`./run.sh`** - скрипти оболонки

## 📁 Структура проекту

```
personal_assistant/
├── models/                 # Моделі даних
│   ├── __init__.py
│   ├── field.py           # Базові класи полів 
│   ├── contact.py         # Клас Contact
│   └── note.py            # Клас Note
├── managers/               # Менеджери для роботи з даними
│   ├── __init__.py
│   ├── contact_manager.py # Управління контактами
│   └── note_manager.py    # Управління нотатками
├── storage/               # Система збереження
│   ├── __init__.py
│   └── file_storage.py    # JSON файлове сховище
├── utils/                 # Допоміжні утиліти
│   ├── __init__.py
│   ├── command_matcher.py # Розпізнавання команд
│   └── validators.py      # Валідація даних
├── cli/                   # Інтерфейс командного рядка
│   ├── __init__.py
│   └── interface.py       # Головний CLI інтерфейс
├── data/                  # Файли даних
│   ├── contacts.json
│   └── notes.json
├── tests/                 # Тести
└── main.py               # Точка входу
```

## 💻 Основні команди

### Управління контактами:
- `add contact` - Додати новий контакт
- `search contact` - Знайти контакт за ім'ям або телефоном
- `show contacts` - Показати всі контакти
- `edit contact` - Редагувати існуючий контакт
- `delete contact` - Видалити контакт
- `birthdays` - Показати найближчі дні народження

### Управління нотатками:
- `add note` - Додати нову нотатку
- `search note` - Знайти нотатку за змістом або тегом
- `show notes` - Показати всі нотатки  
- `edit note` - Редагувати нотатку
- `delete note` - Видалити нотатку

### Загальні команди:
- `help` - Показати довідку
- `exit` або `quit` - Вийти з програми

## 🔧 Використання як Python модуль

```python
from personal_assistant.models import Contact, Note
from personal_assistant.managers import ContactManager, NoteManager
from personal_assistant.storage import FileStorage

# Ініціалізація
storage = FileStorage()
contact_manager = ContactManager(storage)

# Створення контакту
contact = Contact("John Doe")
contact.add_phone("123-456-7890")
contact.add_email("john@example.com")
contact.set_birthday("1990-05-15")

# Збереження
contact_manager.add_contact(contact)

# Пошук
results = contact_manager.search("John")
```

## 🧪 Тестування

```bash
# Запустити всі тести:
python -m pytest

# Запустити тести з поточної директорії:
python tests/run_tests.py

# Швидкий тест функціональності:
python main.py --test
```

## 🎯 Особливості

- **Smart Search**: Розумний пошук з підтримкою часткових збігів
- **Birthday Tracking**: Відслідковування та нагадування про дні народження  
- **Data Validation**: Автоматична валідація телефонів та email
- **Backup System**: Автоматичне резервне копіювання даних
- **Cross-Platform**: Підтримка Windows, Linux, macOS
- **Colored Output**: Кольоровий вивід для кращого сприйняття

## 📋 Системні вимоги

- **Python**: 3.8 або новіший
- **Пам'ять**: Мінімум 50MB RAM
- **Диск**: 10MB вільного місця
- **Залежності**: 
  - `colorama` (опціонально, для кольорів)
  - Стандартна бібліотека Python

## 🤝 Розробка

Для розробки та тестування:

```bash
# Клонувати репозиторій
git clone https://github.com/slaventiysvat/assistant_dev_implementation.git
cd assistant_dev_implementation

# Встановити в режимі розробки
pip install -e .

# Запустити тести
python -m pytest tests/

# Форматування коду (якщо встановлений black)
black . --line-length 88
```

## 📄 Ліцензія

MIT License - деталі в файлі LICENSE

---

**Успішного використання! 🎉**

Для питань та підтримки створіть issue в GitHub репозиторії.