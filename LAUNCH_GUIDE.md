# 🚀 Як запустити Персональний Помічник

## 📋 Швидкий старт

### Windows (найпростіше):
```bash
# Подвійний клік на файл або в командному рядку:
run.bat

# Демонстраційний режим:
run.bat demo

# Тест функціональності:
run.bat test
```

### Linux/macOS:
```bash
# Дайте права на виконання (одноразово):
chmod +x run.sh

# Запуск:
./run.sh

# Демонстраційний режим:
./run.sh demo

# Тест функціональності:
./run.sh test
```

### Універсальний спосіб (Python):
```bash
# Основний запуск:
python main.py

# З параметрами:
python main.py --demo     # Демо режим
python main.py --test     # Тести
python main.py --help-full # Повна довідка
```

---

## 🎂 Основні команди в програмі

### Контакти:
- `add contact` - Додати контакт (тепер з днем народження!)
- `edit contact` - Редагувати контакт (включно з днем народження)
- `birthdays` - Найближчі дні народження
- `show contacts` - Показати всі контакти
- `search contact` - Знайти контакт

### Нотатки:
- `add note` - Додати нотатку
- `show notes` - Показати нотатки
- `search notes` - Знайти нотатки

### Інше:
- `help` - Довідка
- `exit` - Вихід

---

## ⚙️ Системні вимоги

- **Python 3.8+**
- **Windows 10+** / **Linux** / **macOS**
- Віртуальне оточення (рекомендується)

---

## 🛠️ Встановлення залежностей

```bash
# Якщо потрібно встановити залежності:
pip install colorama

# Або використовуйте requirements.txt якщо існує:
pip install -r requirements.txt
```

---

## 📁 Структура файлів

```
dev_implementation/
├── main.py          # ← ГОЛОВНИЙ ФАЙЛ ДЛЯ ЗАПУСКУ
├── run.bat          # ← Windows запуск
├── run.sh           # ← Linux/macOS запуск
├── cli/             # CLI інтерфейс
├── data/            # Дані (contacts.json, notes.json)
├── models/          # Моделі даних
├── managers/        # Менеджери
└── tests/           # Тести
```

---

## 🎯 Приклади використання

### 1. Додати контакт з днем народження:
```
> add contact
Ім'я: Іван Петров
Телефон: +380501234567
Email: ivan@gmail.com
День народження: 15.03.1990  ← НОВЕ!
Адреса: вул. Миру, 10
```

### 2. Знайти найближчі дні народження:
```
> birthdays
На скільки днів наперед: 10
```

### 3. Редагувати день народження:
```
> edit contact
Ім'я: Іван Петров
Редагувати день народження? y
Новий день народження: 20.03.1990
```

---

## ❓ Проблеми та рішення

**Python не знайдено:**
- Встановіть Python з [python.org](https://python.org)
- На Windows переконайтесь що Python доданий до PATH

**Помилка імпорту:**
- Перейдіть до папки `dev_implementation/`
- Запускайте через `python main.py`

**Не працює віртуальне оточення:**
- Створіть: `python -m venv .venv`
- Активуйте: `.venv\Scripts\activate` (Windows) або `source .venv/bin/activate` (Linux)

---

**Готово! Тепер можна запускати програму! 🎉**